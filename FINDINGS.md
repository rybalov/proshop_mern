# FINDINGS — proshop_mern

Top findings sorted by risk. Verified against the codebase on 2026-04-27.q

| #  | Risk | Where                                                           | What                                                                                                                                                  | How to fix                                                                                                   | Status             |
|----|------|-----------------------------------------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------|--------------------|
| 1  | 🔴   | `backend/controllers/orderController.js::updateOrderToPaid`     | Accesses `req.body.payer.email_address` without null-check — crashes the server with `TypeError` on a malformed PayPal callback                       | Guard with `req.body.payer?.email_address` or validate the full payload before assignment                    | ✅ fixed in 11bec2d |
| 2  | 🔴   | `backend/controllers/productController.js::getProducts`         | `req.query.keyword` is passed raw to MongoDB `$regex` — allows ReDoS and potential NoSQL injection                                                    | Escape regex special chars or switch to `$text` index search                                                 | ✅ fixed in a7bcfb2 |
| 3  | 🔴   | `backend/models/userModel.js::pre('save')`                      | Missing `return` after `next()` when password is unmodified — falls through to `bcrypt.hash`, re-hashing an already-hashed value                      | Change `next()` to `return next()`                                                                           | ✅ fixed in 5bf1092 |
| 4  | 🔴   | `backend/middleware/authMiddleware.js::protect`                 | After `next()` in the valid-token `try` block, execution continues to `if (!token)` below — can throw after the response is already in flight         | Add `return` after `next()` inside the `try` block                                                           | ✅ fixed in 25592b0 |
| 5  | 🟡   | `frontend/src/store.js` (initialState hydration)                | Three `JSON.parse(localStorage.getItem(…))` calls with no `try/catch` — corrupted storage crashes the entire React app on boot                        | Wrap each `JSON.parse` in `try/catch`, fall back to the default value                                        | 🔴 not yet         |
| 6  | 🟡   | `package.json`                                                  | `mongoose` ^5.10.6 is 3 major versions behind (current: 8.x); v5 is end-of-life with no security patches                                              | Upgrade to Mongoose 8, remove deprecated options (`useCreateIndex`, `useNewUrlParser`, `useUnifiedTopology`) | 🔴 not yet         |
| 7  | 🟡   | `frontend/package.json`                                         | `react` ^16.13.1 is 2+ majors behind (current: 18/19) — missing concurrent features and security fixes                                                | Upgrade React, ReactDOM, react-scripts; adapt to `createRoot` API                                            | 🔴 not yet         |
| 8  | 🟡   | `backend/controllers/productController.js::createProductReview` | `rating` from `req.body` is cast with `Number()` but never validated for range (1–5), NaN, or negative — saves garbage to DB and corrupts the average | Validate `rating >= 1 && rating <= 5 && !isNaN(rating)` before saving                                        | 🔴 not yet         |
| 9  | 🟡   | `frontend/src/screens/PlaceOrderScreen.js`                      | Tax rate (`0.15`), free-shipping threshold (`100`), shipping cost (`100`) are hardcoded magic numbers                                                 | Extract to `constants/pricing.js` or serve from a backend config endpoint                                    | 🔴 not yet         |
| 10 | 🟡   | `frontend/src/screens/OrderScreen.js` (PayPal script loader)    | `script.onerror` is never set — if the PayPal SDK fails to load the user sees an infinite spinner                                                     | Add `script.onerror` handler that sets an error state                                                        | 🔴 not yet         |
| 11 | 🟡   | `backend/controllers/productController.js::getProducts`         | `pageSize` hardcoded to `10` — clients cannot request a different page size                                                                           | Move to `process.env.PAGE_SIZE` or accept a query param with a sane max                                      | 🔴 not yet         |
| 12 | 🟢   | `backend/controllers/orderController.js::addOrderItems`         | Unreachable `return` after `throw new Error('No order items')` — dead code                                                                            | Remove the `return` line                                                                                     | 🔴 not yet         |
| 13 | 🟢   | `backend/middleware/authMiddleware.js::protect`                 | `console.error(error)` in the catch block — leaks stack traces in production logs                                                                     | Remove or replace with a structured logger                                                                   | 🔴 not yet         |
| 14 | 🟢   | `frontend/src/screens/OrderScreen.js::successPaymentHandler`    | `console.log(paymentResult)` left in — debug output shipped to production                                                                             | Remove the `console.log` call                                                                                | 🔴 not yet         |
| 15 | 🟢   | `frontend/src/screens/OrderScreen.js`                           | Component is 234 lines — mixes PayPal SDK loading, payment, delivery status, and order display in one function                                        | Split into sub-components: `PayPalLoader`, `OrderSummary`, `DeliveryStatus`                                  | 🔴 not yet         |
